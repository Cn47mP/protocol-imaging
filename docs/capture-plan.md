# 采集方案

> 更新时间：2026-05-29
> 项目：协议映射

## 概述

对终末地 AIC 基地做俯视截图采集，并将多帧拼接为完整全景图。工具已从独立 PySide6 GUI 重构为 CLI，作为 MaaEnd 的子模块运行。

## 当前实现

- 采集方式：通过 MaaEnd Pipeline + Go Service 调度
- 视角控制：CharacterControllerPitchDeltaAction 拉远视角
- 网格采集：Go Service 蛇形网格移动 + MaaFramework 截图
- 模糊检测：Laplacian 方差，低于阈值自动跳过
- 全景拼接：ORB 特征匹配 + 高斯羽化融合（Python CLI）
- 导出：PNG

## 待校准参数（需要真实 AIC 实测）

| 参数 | 当前估算 | 说明 |
|------|----------|------|
| 单帧覆盖格数 | ~8×5 格（1920×1080） | 需要进入 AIC 最高俯视角度实测 |
| 满扩建基地范围 | ~36×40 格 | 需要目测校准 |
| 移动步长 | 按 0.35s WASD 按键持续 | 需要实测修改 DEFAULT_PAN_DURATION |
| 缩放步数 | 10 步滚轮 | 需要确认拉到最远需要多少步 |

## 路线

| 阶段 | 状态 |
|------|------|
| P0 自动采集 | 已合入 |
| P1 体验优化 | 已合入 |
| P2 MaaEnd 集成 | 已合入 |